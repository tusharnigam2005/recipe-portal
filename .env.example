# Server
PORT=3000

# Default Search Console configuration (optional fallbacks).
# The Developer Console form always takes priority over these values -
# they only apply if a field is left blank.
SEARCH_CONSOLE_ENDPOINT=
SEARCH_CONSOLE_API_TOKEN=

# Request timeout (ms) when pushing recipes to the external Search Console API
PUSH_TIMEOUT_MS=15000
